from setuptools import setup

setup(name='backtest_post_processing',
      version='0.1',
      description='Simple python package template',
      author='All',
      author_email='nomail@passp.com',
      packages=['backtest_post_processing'],
      install_requires = [
          'numpy',
      ],
      zip_safe=False)
